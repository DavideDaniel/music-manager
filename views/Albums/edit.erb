<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" type="text/css" href="/styles.css">
  <meta charset="UTF-8">
  <title>Edit <%= album.title %></title>
</head>
<body>
  <form action="/album/<%= album.id %>" method="POST">
    <input type="hidden" name="_method" value="PUT">
    <input type="text" name="name" placeholder="Name" value="<%= album.title %>" />
    <select name="artist_id">
      <% artists.each do |artist| %>
        <option value="<%= artist.id %>" <%= "selected" if album.artist.id == artist.id%> ><%= artist.name %></option>
      <% end %>
    </select>
    <button>Submit</button>
  </form>

  <form action="/album/<%= album.id %>" method="POST">
    <input type="hidden" name="_method" value="DELETE">
    <button>Delete Album</button>
  </form>
</body>
</html>